spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/school
    username: postgres
    password: 123
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

  security:
    oauth2:
      client:
          registration:
            keycloak:
              client-id: microservice
              client-secret: Ky6SLS2C2xgwsHmkXi4QxfiwStJcFDct
              provider: keycloak
              authorization-grant-type: authorization_code
              redirect-uri: http://localhost:8080/login/oauth2/code/keycloak
              scope: openid, profile
          provider:
            keycloak:
              issuer-uri: http://localhost:8081/realms/master

      resourceserver:
        jwt:
          issuer-uri: http://localhost:8081/realms/master
          principal-claim-name: preferred_username
          audiences:
            - microservice